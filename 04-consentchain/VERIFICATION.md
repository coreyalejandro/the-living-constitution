# Verification — ConsentChain (TLC)

Machine-checkable family verification is run from The Living Constitution root:

```bash
python3 scripts/verify_consentchain_family.py --root .
```

Reports are written to `verification/consentchain-family/report.json` and `report.md`. This document summarizes expectations; the reports are the source of truth for each run.
