# OpenMemory guide — the-living-constitution

## Overview

Governance overlay / super-repo for Safety Systems Design Commonwealth: constitutional spec, build contracts per project, verification matrix.

## Components

- **`scripts/verify_consentchain_family.py`** — ConsentChain family verifier: TLC paths under `projects/consentchain/`, optional `--fix-git` / `--update-existing`, repo checks, reports under `verification/consentchain-family/`. Stricter artifact lists via `--config` JSON.

## User Defined Namespaces

- (default) verification
