projects/teaser-video-remotion — C-RSP verification pass (2026-03-31)

1. cd projects/teaser-video-remotion
2. npm run lint  (tsc --noEmit) — exit 0 before and after render
3. bash scripts/preflight-render-check.sh — exit 0
4. npm run render — exit 0 (bash scripts/render.sh)
5. shasum -a 256 dist/im-just-a-build.mp4 — recorded in OUTPUT_SHA256.txt
6. Frame spot-check: ffmpeg raw rgb24 pipe | shasum for frame indices 0,180,360,539,719 — FRAME_HASHES.txt
7. ffprobe dist/im-just-a-build.mp4 — 1080x1080, 12 fps, 720 frames, ~60s (see RENDER_REPORT.md)

First render attempt (before render.sh fix): FAILED — Remotion error visiting http://localhost:3001/index.html (no response). Root cause: HTTP(S) proxy routing localhost away from Remotion’s bundle server.

Correction applied: scripts/render.sh — NO_PROXY/no_proxy for 127.0.0.1,localhost; flags --ipv4 --timeout=120000 --concurrency=1; optional --browser-executable when Chrome/Chromium path exists.

Second render attempt (after fix): SUCCESS — dist/im-just-a-build.mp4 produced.
