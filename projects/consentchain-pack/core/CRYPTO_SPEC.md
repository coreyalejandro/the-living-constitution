# Cryptographic Specification — ConsentChain

This document captures the **constitutional-level** cryptographic intent for ConsentChain (ledger semantics, consent binding, key handling). Detailed algorithms and code references live in the implementation repo.

Update this file when the TLC-level contract for cryptography changes.
