# CLAUDE.md — Document System

## What This Is

TLC C-RSP instance overlay for TLC document-system constitutionalization (`BUILD_CONTRACT.instance.md` and related artifacts). Inherits the root `CLAUDE.md` and `THE_LIVING_CONSTITUTION.md`.

## Domain

Documentation governance, MVDS, and constitutional doc companions under `docs/constitution/`.

## Rules

All Living Constitution Articles I–V apply. Truth for doc enforcement lives in `scripts/verify_document_constitution.py` and `config/docs_governance.json` as referenced by the instance contract.
