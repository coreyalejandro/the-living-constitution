# Operator Handoff Instructions

Open this folder at the TLC repo root or copy it into `projects/im-just-a-build/`.

Then hand `BUILD_CONTRACT.md` to your coding operator and instruct it to execute the contract in a single pass.

Recommended operator for local execution: **Cursor**.

Minimum environment:
- Node.js 18+
- npm
- FFmpeg
