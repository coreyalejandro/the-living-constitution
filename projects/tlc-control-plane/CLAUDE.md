# CLAUDE.md — TLC Control Plane (Sandbox Satellite)

## What This Is

Governed UI / control-plane satellite: dual `PROJECT_TOPOLOGY.json`, `src/app.py` (mirrored to `core/`), and `governance/` PASS 8 artifacts. Entry for governed execution is via `SandboxEngine` from `projects/sandbox-runtime/` — not ad-hoc `python app.py`.

## Domain

Cross-domain governance UI and enforcement-map alignment (INVARIANT lattice).

## Rules

All Living Constitution Articles I–V apply. See `projects/sandbox-runtime/CLAUDE.md` and root `openmemory.md` for sandbox halt and topology verification commands.
